{
  "name": "cf-ai-customer-support",
  "version": "1.0.0",
  "description": "Minimal AI customer support with Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
